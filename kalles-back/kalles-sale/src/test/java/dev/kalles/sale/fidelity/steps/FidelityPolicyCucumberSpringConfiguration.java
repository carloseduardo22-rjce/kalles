package dev.kalles.sale.fidelity.steps;

import dev.kalles.sale.fidelity.support.AbstractFidelityPolicyApiSupport;
import io.cucumber.spring.CucumberContextConfiguration;

@CucumberContextConfiguration
public class FidelityPolicyCucumberSpringConfiguration extends AbstractFidelityPolicyApiSupport {
}
